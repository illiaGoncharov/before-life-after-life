# Настройка отправки данных формы на GitHub Pages

## Текущая реализация

Проект использует **EmailJS** для отправки данных формы. Это идеальное решение для статического хостинга GitHub Pages, так как:
- ✅ Работает полностью на клиенте (браузере)
- ✅ Не требует серверной части
- ✅ Бесплатный тариф позволяет отправлять до 200 писем/месяц
- ✅ Легко настроить

## Как это работает

1. **Клиент заполняет форму** → данные сохраняются в localStorage (прогресс)
2. **При отправке формы** → данные отправляются через EmailJS API
3. **EmailJS обрабатывает запрос** → отправляет email на указанный адрес
4. **Изображения не отправляются** → только их количество (ограничение EmailJS)

## Настройка EmailJS

### Шаг 1: Регистрация и создание сервиса

1. Зарегистрируйтесь на [EmailJS.com](https://www.emailjs.com/)
2. Создайте Email Service (Gmail, Outlook, или другой)
3. Создайте Email Template с переменными:
   - `{{from_name}}` - имя пользователя
   - `{{from_email}}` - email пользователя
   - `{{origin}}` - происхождение
   - `{{custom_prompt}}` - пользовательский промпт
   - `{{payment}}` - способ оплаты
   - `{{train_ai}}` - согласие на обучение AI
   - `{{publicly}}` - согласие на публичное использование
   - `{{images_count}}` - количество изображений
   - `{{timestamp}}` - время отправки
   - `{{message}}` - полные данные в JSON формате

### Шаг 2: Получение credentials

После создания сервиса и шаблона вам понадобятся:
- **Service ID** - ID вашего email сервиса
- **Template ID** - ID вашего шаблона
- **Public Key** - публичный ключ из настройков аккаунта

### Шаг 3: Настройка переменных окружения

Для локальной разработки создайте файл `.env` в корне проекта:

```env
REACT_APP_EMAILJS_SERVICE_ID=your_service_id
REACT_APP_EMAILJS_TEMPLATE_ID=your_template_id
REACT_APP_EMAILJS_PUBLIC_KEY=your_public_key
```

⚠️ **Важно**: Не коммитьте `.env` файл в git! Добавьте его в `.gitignore`.

### Шаг 4: Настройка для GitHub Pages

Для GitHub Pages нужно настроить переменные окружения в GitHub Actions или использовать другой подход:

#### Вариант 1: GitHub Secrets (рекомендуется)

1. Перейдите в Settings → Secrets and variables → Actions
2. Добавьте секреты:
   - `REACT_APP_EMAILJS_SERVICE_ID`
   - `REACT_APP_EMAILJS_TEMPLATE_ID`
   - `REACT_APP_EMAILJS_PUBLIC_KEY`

3. Создайте GitHub Actions workflow для автоматической сборки с переменными окружения

#### Вариант 2: Встроить в код (небезопасно, но работает)

Можно временно встроить значения напрямую в код для тестирования:

```javascript
const EMAILJS_CONFIG = {
  serviceId: 'your_service_id',
  templateId: 'your_template_id',
  publicKey: 'your_public_key',
};
```

⚠️ **Не рекомендуется для продакшена**, так как публичный ключ будет виден всем.

#### Вариант 3: Использовать build-time переменные

В `package.json` можно добавить скрипт для сборки с переменными:

```json
{
  "scripts": {
    "build": "REACT_APP_EMAILJS_SERVICE_ID=xxx REACT_APP_EMAILJS_TEMPLATE_ID=yyy REACT_APP_EMAILJS_PUBLIC_KEY=zzz react-scripts build"
  }
}
```

## Ограничения

### Текущие ограничения EmailJS:

1. **Изображения не отправляются** - только количество
   - 📸 Изображения хранятся только локально (localStorage)
   - 📊 В email приходит только количество загруженных изображений
   - 💡 **Решение**: Если нужна отправка изображений, можно:
     - Использовать Cloudinary/Firebase Storage для загрузки
     - Отправлять ссылки на изображения в email
     - Или использовать другой сервис (Formspree, Netlify Forms)

2. **Лимит бесплатного тарифа**: 200 писем/месяц
   - Для большего объема нужен платный тариф

3. **Публичный ключ виден в коде**
   - Это нормально для EmailJS (это публичный ключ)
   - Но лучше использовать переменные окружения

## Альтернативные решения

Если EmailJS не подходит, можно использовать:

### 1. Formspree
- ✅ Простая форма отправки
- ✅ Поддержка файлов
- ✅ Бесплатный тариф: 50 писем/месяц

### 2. Netlify Forms
- ✅ Бесплатно для сайтов на Netlify
- ✅ Поддержка файлов
- ⚠️ Требуется хостинг на Netlify

### 3. Firebase Functions + Storage
- ✅ Полный контроль
- ✅ Поддержка файлов
- ⚠️ Требует настройки Firebase

### 4. Google Apps Script
- ✅ Бесплатно
- ✅ Интеграция с Gmail
- ⚠️ Требует настройки скрипта

## Рекомендации

1. **Для начала**: Используйте EmailJS с GitHub Secrets
2. **Для продакшена**: Рассмотрите Firebase Storage для изображений + EmailJS для данных
3. **Для тестирования**: Можно временно использовать встроенные значения

## Текущее состояние

✅ Реализовано:
- Отправка данных формы через EmailJS
- Сохранение прогресса в localStorage
- Обработка ошибок отправки

⚠️ Требует настройки:
- Настройка EmailJS аккаунта
- Добавление переменных окружения
- Тестирование отправки

📝 TODO:
- Настроить EmailJS сервис и шаблон
- Добавить переменные окружения
- Протестировать отправку на GitHub Pages
- Рассмотреть решение для отправки изображений (если требуется)

---
Последнее обновление: 2025-01-XX






